━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📚 第 18 章：全文本搜索
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📖 学习目标
──────────────────────────────────────
✓ 理解全文本搜索的概念和优势
✓ 掌握 FULLTEXT 索引的使用
✓ 学会使用 MATCH() 和 AGAINST()
✓ 了解布尔模式和查询扩展

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📝 核心知识点
──────────────────────────────────────

1️⃣  理解全文本搜索
   ────────────────────────────────────
   LIKE 和 REGEXP 的局限性：
   • 性能差（需扫描所有行）
   • 无法智能结果排序
   • 不能指定词的重要性
   
   全文本搜索优势：
   • 性能好（使用索引）
   • 智能结果排序（相关性排名）
   • 支持自然语言搜索


2️⃣  启用全文本搜索支持
   ────────────────────────────────────
   创建表时指定FULLTEXT：
   
   CREATE TABLE productnotes (
     note_id INT NOT NULL AUTO_INCREMENT,
     prod_id CHAR(10) NOT NULL,
     note_date DATETIME NOT NULL,
     note_text TEXT NULL,
     PRIMARY KEY(note_id),
     FULLTEXT(note_text)
   ) ENGINE=MyISAM;
   
   注意：
   • 只有MyISAM引擎支持FULLTEXT（MySQL 5.6+的InnoDB也支持）
   • crashcourse的productnotes表已经有FULLTEXT索引


3️⃣  进行全文本搜索
   ────────────────────────────────────
   SELECT note_text
   FROM productnotes
   WHERE MATCH(note_text) AGAINST('rabbit');
   
   说明：
   • MATCH()指定要搜索的列
   • AGAINST()指定搜索词
   • 返回包含'rabbit'的所有行


4️⃣  全文本搜索 vs LIKE
   ────────────────────────────────────
   LIKE方式：
   SELECT note_text
   FROM productnotes
   WHERE note_text LIKE '%rabbit%';
   
   全文本搜索方式：
   SELECT note_text
   FROM productnotes
   WHERE MATCH(note_text) AGAINST('rabbit');
   
   区别：
   • 结果相同，但顺序不同
   • 全文本搜索按相关性排序
   • 全文本搜索性能更好


5️⃣  查询扩展
   ────────────────────────────────────
   SELECT note_text
   FROM productnotes
   WHERE MATCH(note_text) 
         AGAINST('anvils' WITH QUERY EXPANSION);
   
   工作原理：
   1. 首先进行基本全文本搜索
   2. 检查匹配行，找出有用的词
   3. 使用这些词再次搜索
   
   结果：返回更多相关行


6️⃣  布尔文本搜索
   ────────────────────────────────────
   SELECT note_text
   FROM productnotes
   WHERE MATCH(note_text) 
         AGAINST('heavy -rope*' IN BOOLEAN MODE);
   
   布尔操作符：
   +     包含，必须存在
   -     排除，必须不存在
   >     提高排名值
   <     降低排名值
   ()    分组
   *     词尾通配符
   ""    定义短语


7️⃣  布尔操作符示例
   ────────────────────────────────────
   搜索包含'rabbit'或'carrot'的行：
   AGAINST('rabbit carrot' IN BOOLEAN MODE)
   
   搜索包含'rabbit'和'carrot'的行：
   AGAINST('+rabbit +carrot' IN BOOLEAN MODE)
   
   搜索包含'rabbit'但不包含'carrot'的行：
   AGAINST('+rabbit -carrot' IN BOOLEAN MODE)
   
   搜索短语'rabbit carrot'：
   AGAINST('"rabbit carrot"' IN BOOLEAN MODE)


8️⃣  全文本搜索的使用说明
   ────────────────────────────────────
   限制：
   • 短词被忽略（3个字符以下）
   • 内建非用词（stopword）列表被忽略（如the、a等）
   • 出现在50%以上行的词被视为非用词
   • 表中行数<3时，全文本搜索不返回结果
   • 单引号被忽略

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎯 实践任务
──────────────────────────────────────

📝 基础练习
──────────────────────────────────────

练习 1: 基本全文本搜索
──────────────────────────────────────
搜索包含'rope'的产品备注

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('rope');


练习 2: 查看相关性得分
──────────────────────────────────────
显示搜索'rabbit'的相关性排名

你的SQL：
  SELECT note_id, note_text,
         MATCH(note_text) AGAINST('rabbit') AS rank
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('rabbit')
  ORDER BY rank DESC;


练习 3: 布尔模式搜索
──────────────────────────────────────
搜索包含'anvil'但不包含'rope'的备注

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('+anvil -rope' IN BOOLEAN MODE);


练习 4: 多词搜索
──────────────────────────────────────
搜索包含'safe'或'easy'的备注

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('safe easy' IN BOOLEAN MODE);


练习 5: 短语搜索
──────────────────────────────────────
搜索包含短语'very popular'的备注

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('"very popular"' IN BOOLEAN MODE);

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🚀 进阶挑战
──────────────────────────────────────

挑战 1: 复杂布尔搜索
──────────────────────────────────────
搜索：必须包含'safe'，最好包含'recommended'，不包含'danger'

你的SQL：
  SELECT note_text,
         MATCH(note_text) AGAINST('+safe recommended -danger' 
                                  IN BOOLEAN MODE) AS score
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('+safe recommended -danger' 
                                 IN BOOLEAN MODE)
  ORDER BY score DESC;


挑战 2: 通配符搜索
──────────────────────────────────────
搜索以'cust'开头的所有词

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('cust*' IN BOOLEAN MODE);


挑战 3: 查询扩展应用
──────────────────────────────────────
使用查询扩展搜索'heavy'

你的SQL：
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('heavy' WITH QUERY EXPANSION)
  LIMIT 5;


挑战 4: 多列全文本搜索
──────────────────────────────────────
如果有多个文本列，搜索它们（假设场景）

你的SQL：
  -- 示例（productnotes只有一个FULLTEXT列）
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('anvil');
  
  -- 多列语法示例：
  -- WHERE MATCH(col1, col2) AGAINST('search_term');


挑战 5: 相关性排序
──────────────────────────────────────
找出最相关的产品备注（按相关性降序）

你的SQL：
  SELECT note_id, prod_id,
         MATCH(note_text) AGAINST('anvil') AS relevance,
         note_text
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('anvil')
  ORDER BY relevance DESC
  LIMIT 3;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💡 重要提示
──────────────────────────────────────

⚠️  引擎支持
   MySQL 5.6之前：只有MyISAM支持FULLTEXT
   MySQL 5.6+：InnoDB也支持FULLTEXT
   crashcourse的productnotes使用MyISAM

⚠️  性能优化
   全文本搜索使用索引，比LIKE快得多
   但创建FULLTEXT索引需要时间
   数据导入时建议先导入数据，后建索引

⚠️  短词限制
   默认忽略3个字符以下的词
   可以通过ft_min_word_len变量修改
   修改后需要重建索引

⚠️  50%规则
   如果词出现在50%以上的行中，将被视为非用词
   在小数据集上测试时要注意这个限制
   可以使用布尔模式避免50%规则

⚠️  布尔模式的特殊性
   布尔模式不需要FULLTEXT索引也能工作
   但有索引性能会更好
   布尔模式不按相关性排序（除非明确指定）

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 实战示例
──────────────────────────────────────

示例 1: 基本搜索和排序
  SELECT note_text,
         MATCH(note_text) AGAINST('rabbit') AS score
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('rabbit')
  ORDER BY score DESC;


示例 2: 复杂布尔表达式
  SELECT note_text
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('+anvil +(>safe <danger)' IN BOOLEAN MODE);


示例 3: 查询扩展对比
  -- 不使用扩展
  SELECT COUNT(*) AS normal_count
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('anvil');
  
  -- 使用扩展
  SELECT COUNT(*) AS expanded_count
  FROM productnotes
  WHERE MATCH(note_text) 
        AGAINST('anvil' WITH QUERY EXPANSION);


示例 4: 组合产品信息
  SELECT p.prod_id, p.prod_name, pn.note_text
  FROM products p
  JOIN productnotes pn ON p.prod_id = pn.prod_id
  WHERE MATCH(pn.note_text) AGAINST('safe' IN BOOLEAN MODE);


示例 5: 搜索结果分页
  SELECT note_id, note_text,
         MATCH(note_text) AGAINST('anvil') AS score
  FROM productnotes
  WHERE MATCH(note_text) AGAINST('anvil')
  ORDER BY score DESC
  LIMIT 5 OFFSET 0;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

🎓 知识检查点
──────────────────────────────────────

□ 你理解全文本搜索的优势吗？
□ 你知道如何创建FULLTEXT索引吗？
□ 你会使用MATCH()和AGAINST()吗？
□ 你了解布尔模式的操作符吗？
□ 你理解查询扩展的工作原理吗？
□ 你知道全文本搜索的限制吗？
□ 你会查看搜索的相关性得分吗？
□ 你了解50%规则的影响吗？

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

✨ 本章小结
──────────────────────────────────────

你已经学会了：
✓ 理解全文本搜索的概念和优势
✓ 使用FULLTEXT索引提高搜索性能
✓ 使用MATCH()和AGAINST()进行搜索
✓ 使用布尔模式的各种操作符
✓ 使用查询扩展获得更多结果
✓ 理解全文本搜索的限制和规则
✓ 查看和使用相关性排名

下一章将学习如何插入数据！

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

💻 动手实践
──────────────────────────────────────
现在就打开 MySQL，在 crashcourse 数据库中
试试全文本搜索功能吧！

选择 "1. 进入 MySQL 实践" 开始练习
